const config = {
    API_URL: process.env.REACT_APP_API_URL,
    // TODO: pon aquí la URL de tu app cuando esté publicada (p.ej. "https://app.subtitlepro.com")
    APP_URL: '#',
    BRAND_NAME: 'SubtitlePro',
};

export default config;
